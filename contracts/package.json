{
  "name": "blockfinax-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for BlockFinaX decentralized trade finance platform",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy-diamond.js --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy-diamond.js --network base",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}
